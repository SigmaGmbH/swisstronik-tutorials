{
  "name": "perc20-example",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test --network swisstronik",
    "deploy": "hardhat run scripts/deploy.ts --network swisstronik"
  }
}
